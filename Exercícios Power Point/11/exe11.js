var catAd = prompt("Digite o valor do cateto adjacente:");
var catOp = prompt("Digite o valor do cateto oposto:");

hipotenusa = Math.sqrt((catAd*catAd)+(catOp*catOp));

alert(`O valor da hipotenusa é ${hipotenusa}`);